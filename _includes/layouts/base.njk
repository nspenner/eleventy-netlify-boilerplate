<!doctype html>
<html {% if section %} data-current="{{ section }}"{% endif %} lang="en">

	{% include "components/head.njk" %}

	<body>

		<header>
			<h1>
				<a class="desktop-display" href="{{ '/' | url }}">{{ metadata.title }}</a>
				<a class="mobile-display" href="{{ '/' | url }}">{{ metadata.abbreviatedTitle }}</a>
			</h1>
			{% include "components/nav.njk" %}
		</header>

		<main>
			{{ layoutContent | safe }}
		</main>

		{% include "components/footer.njk" %}

	</body>

</html>
